package com.ocsr.service;

import com.ocsr.bean.LoginBean;
import com.ocsr.dao.LoginDAO;

public class LoginService {
public String validateCredentials(LoginBean bean){
	LoginDAO loginDAO=new LoginDAO();
//	System.out.println(loginDAO.validateCredentials(bean));
	return loginDAO.validateCredentials(bean);
	
}
}
